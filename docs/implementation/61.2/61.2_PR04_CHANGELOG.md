# DentalOperix 61.2 - PR-61.2-04A Changelog

Date: 2026-06-22
Scope: Assistant / Front Desk Workspace
PR: PR-61.2-04A Lead Detail Read-Only

## Added

- Added `src/components/assistant/LeadDetailPanel.tsx`.
- Added read-only Lead Detail view for Front Desk users.
- Added display support for lead identification, contact data, treatment, source, preferred date, message, notes, AI summary, calendar event ID, and email-sent metadata when available.
- Added local navigation from Lead Queue to Lead Detail and back.

## Changed

- Updated `src/components/assistant/LeadQueueWidget.tsx` to support selecting a lead and rendering the read-only detail panel.
- Extended `LeadQueueItem` type to include optional read-only fields already compatible with the certified Lead payload.
- Updated `src/components/assistant/LeadQueueWidget.test.tsx` with PR-61.2-04A read-only detail coverage.
- Updated `src/components/assistant/AssistantDashboard.test.tsx` with Dashboard integration coverage for Lead Detail.

## Not Changed

- No backend endpoint was changed.
- No persistence adapter was changed.
- No write pathway was added.
- `/api/leads/list` remains the Lead Queue read source.
- `/api/leads/create` was not modified.
- Today's Schedule was not changed and continues to consume `useAppointments()` / `appointments-store`.

## Governance

- Leads = Source of Truth remains intact.
- Certified persistence architecture remains unchanged.
- Protected components were not modified.
- No prohibited architecture pattern was introduced.

## Validation

Automated test command attempted:

```bash
npm test -- src/components/assistant/LeadQueueWidget.test.tsx src/components/assistant/AssistantDashboard.test.tsx
```

Execution blocked in the provided ZIP environment because `vitest` is unavailable without `node_modules`.
