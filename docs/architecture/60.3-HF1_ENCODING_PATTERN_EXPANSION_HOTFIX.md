# 60.3-HF1 Encoding Pattern Expansion Hotfix

## Status

PROGRAM: 60.x Clinical Intelligence  
PHASE: 60.3-HF1 Encoding Pattern Expansion  
STATUS: IMPLEMENTED  
TYPE: Read-only text normalization hotfix  
ARCHITECTURE CHANGE: NO  
SOURCE OF TRUTH CHANGE: NO  
PERSISTENCE CHANGE: NO  
DUAL WRITE: NO

## Governing Baseline

This hotfix is governed by:

- `docs/architecture/57.9_DOCUMENTATION_CONSOLIDATION_AND_PROGRAM_CLOSURE.md`
- `docs/architecture/60.3_DATA_QUALITY_AND_ENCODING_AUDIT.md`

Certified source of truth remains unchanged:

```text
Leads = Source of Truth
```

Certified persistence architecture remains unchanged:

```text
Leads
  -> LeadPersistencePort
  -> LeadPersistenceProvider
  -> RelationalLeadPersistenceAdapter
  -> Supabase PostgreSQL
```

## 1. Architectural Analysis

60.3 introduced a conservative read-side mojibake normalizer. Local validation showed that some legacy CRM rows contain incomplete mojibake patterns, for example:

```text
OdontologÃa Preventiva
Roxana MartÃnes
```

These values do not match the complete UTF-8 mojibake sequences handled by the first 60.3 implementation, such as:

```text
RevisiÃ³n Dental
DiseÃ±o de Sonrisa
```

60.3-HF1 expands the normalizer with contextual replacements for observed incomplete patterns only. The change remains read-only and does not rewrite Google Sheets, Supabase, or any certified persistence source.

## 2. Affected Dependencies

Updated:

```text
src/lib/text-normalization.ts
```

Validation target:

```text
src/lib/text-normalization.test.ts
```

Protected components were not modified:

```text
BookingDialog
processDentalLead
/api/leads/create
Calendar
Gmail
FloatingDentalAIChat
Home
siteServices.ts
```

## 3. Risk Assessment

### Risk: over-normalization

The hotfix does not introduce broad character conversion. It uses contextual replacements for confirmed patterns only:

```text
ologÃa -> ología
MartÃnes -> Martínes
AlimaÃ±a -> Alimaña
RevisiÃn -> Revisión
```

### Risk: persistence mutation

None. The normalization is applied on read/display/analytics paths only.

## 4. Technical Impact

```text
Persistence: 0
Source of Truth: 0
Google Sheets write path: 0
Lead creation path: 0
Read-side normalization: IMPROVED
Analytics grouping: IMPROVED
UI display quality: IMPROVED
```

## 5. Validation Plan

Run:

```text
npm test -- --run src/lib/text-normalization.test.ts
```

Expected corrected examples:

```text
OdontologÃa Preventiva -> Odontología Preventiva
Roxana MartÃnes -> Roxana Martínes
DiseÃ±o de Sonrisa -> Diseño de Sonrisa
RevisiÃ³n Dental -> Revisión Dental
Juanito AlimaÃ±a -> Juanito Alimaña
```

In the generation environment, tests could not be executed because `node_modules` is not present.

## Governance Certification

```text
Leads = Source of Truth: PRESERVED
Persistence transition 57.x: NOT REOPENED
Dual Write: NOT INTRODUCED
Product Migration: NOT INTRODUCED
Lead Replacement: NOT INTRODUCED
Protected Components: NOT MODIFIED
```
