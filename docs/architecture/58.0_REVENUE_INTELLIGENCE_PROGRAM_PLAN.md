# 58.0 Revenue Intelligence Program Plan

## Status

PROGRAM: 58.x Revenue Intelligence
STATUS: INITIATED
GOVERNANCE: ACTIVE
ARCHITECTURE CHANGE: NO
SOURCE OF TRUTH CHANGE: NO
DUAL WRITE: NO
PRODUCT MIGRATION: NO

## Governing Baseline

This program is governed by:

- `docs/architecture/57.9_DOCUMENTATION_CONSOLIDATION_AND_PROGRAM_CLOSURE.md`

The 57.x persistence transition remains closed and certified. Revenue Intelligence must not reopen persistence governance, change the certified source of truth, or introduce an alternate master data source.

Certified baseline:

```text
Leads
  -> LeadPersistencePort
  -> LeadPersistenceProvider
  -> RelationalLeadPersistenceAdapter
  -> Supabase PostgreSQL
```

Certified source of truth:

```text
Leads = Source of Truth
```

## Program Objective

Revenue Intelligence converts existing lead and CRM operational data into governed read-only business metrics.

The program does not create master records, does not persist alternate lead state, and does not replace the certified lead persistence architecture.

## Architectural Position

```text
Leads Source of Truth
  -> Read / Analytics Calculations
  -> Revenue Intelligence Contract
  -> Dashboards / Reports / Insights
```

Revenue Intelligence is a derived analytics layer.

## Protected Components

The program must not modify:

- `BookingDialog`
- `processDentalLead`
- `/api/leads/create`
- Calendar integration
- Gmail integration
- `FloatingDentalAIChat`
- `Home`
- `siteServices.ts`

## Initial Scope

58.2 introduces `RevenueSnapshotV1`, a typed read-only contract that centralizes existing CRM and commercial KPI formulas into one governed output shape.

## Out of Scope

- New persistence tables
- Dual write
- Product migration
- Lead replacement
- New source of truth
- Mutation of lead creation or booking flows
- Modification of certified 57.x architecture

## Governance Notes

Any future endpoint, dashboard integration, or report must consume Revenue Intelligence as a derived read model and must explicitly preserve the 57.9 certified baseline.

## 58.3 Update - Revenue Intelligence Integration Layer

STATUS: IMPLEMENTED

58.3 introduced a read-only integration boundary for the 58.2 Revenue Intelligence contract.

Implemented boundary:

```text
GET /api/analytics/revenue
```

Implemented client adapter:

```text
src/lib/api/revenue-intelligence.ts
```

Governance result:

- ARCHITECTURE CHANGE: NO
- SOURCE OF TRUTH CHANGE: NO
- PERSISTENCE CHANGE: NO
- DUAL WRITE: NO
- PRODUCT MIGRATION: NO
- PROTECTED COMPONENT CHANGES: NO

58.4 is reserved for dashboard alignment against the Revenue Intelligence integration boundary.


## 58.4 Update - Dashboard Alignment

STATUS: IMPLEMENTED

58.4 aligned dashboard consumers to the 58.3 Revenue Intelligence integration boundary.

Implemented adapter:

```text
src/lib/api/revenue-dashboard-metrics.ts
```

Implemented route registration:

```text
GET /api/analytics/revenue
```

Migrated consumers:

```text
src/routes/admin/dashboard.tsx
```

Governance result:

- ARCHITECTURE CHANGE: NO
- SOURCE OF TRUTH CHANGE: NO
- PERSISTENCE CHANGE: NO
- DUAL WRITE: NO
- PRODUCT MIGRATION: NO
- PROTECTED COMPONENT CHANGES: NO

58.4 keeps existing dashboard component contracts through a compatibility adapter while making `RevenueSnapshotV1` the primary dashboard KPI source.

## 58.5 Update - Revenue Forecasting

STATUS: IMPLEMENTED

58.5 introduced deterministic read-only forecasting over the governed `RevenueSnapshotV1` contract.

Implemented contract:

```text
RevenueForecastSnapshot
```

Implemented endpoint:

```text
GET /api/analytics/revenue-forecast
```

Implemented client:

```text
src/lib/api/revenue-forecast.ts
```

Governance result:

- ARCHITECTURE CHANGE: NO
- SOURCE OF TRUTH CHANGE: NO
- PERSISTENCE CHANGE: NO
- DUAL WRITE: NO
- PRODUCT MIGRATION: NO
- PROTECTED COMPONENT CHANGES: NO

58.5 remains a derived analytics capability. It produces estimated forecast values only and does not introduce actual revenue recognition, payment state, or a new master-data source.

## 58.x Closure Note - Executive Analytics Handoff

STATUS: REVENUE INTELLIGENCE CAPABILITY COMPLETE

After 58.5, Revenue Intelligence provides:

```text
RevenueSnapshotV1
RevenueForecastSnapshot
Revenue Analytics APIs
Dashboard Alignment
```

Program 59 builds on these read-only outputs. It must not consume persistence directly as its primary architectural contract and must not reopen the certified 57.x baseline.
