# PR-61.2-01 / PR-61.2-02 AI Task Assignments

## Cursor

### Task

Apply the reconciled code update package to the active branch.

### Required checks

- Confirm modified files match the implementation summary.
- Confirm no protected components were modified.
- Confirm `/assistant` is still protected by `RoleRouteGuard allowedRoles={["assistant"]}`.
- Confirm Assistant dashboard does not import PatientManagementWorkspace or clinical record modules.

## Claude

### Task

Review architecture and governance compliance.

### Required checks

- Validate that Leads remain Source of Truth.
- Validate that Today's Schedule consumes `useAppointments()` / `src/lib/appointments-store.ts` and not `/api/leads/list`.
- Validate no Patient Management, Clinical Records, assignments, retention, soft delete, global search, or real-time updates were introduced.
- Validate that RBAC remains authorization only.

## ChatGPT

### Task

Prepare next-step planning for PR-61.2-03 Lead Queue after CI confirmation.

### Preconditions

- PR-61.2-01/02 update package applied.
- Full CI completed locally.
- Lockfile synchronization resolved.
- Governance closure report updated with validation evidence.

## QA / Test AI

### Task

Run and record validation evidence.

### Commands

```bash
npm ci
npm test -- --run
npm run build
```

If `npm ci` fails due lockfile mismatch, report as environment/package hygiene issue before continuing.

## Stop Conditions

Stop and request Architecture Review if implementation requires any of:

- Patient Management
- Clinical Records
- Doctor assignment
- Lead assignment
- Lead to Patient relationship model
- Retention policy
- Soft delete policy
- Real-time updates
- Global search
- Persistence re-architecture
