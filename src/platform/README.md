# Platform Source Assets

Canonical folder for platform-level capabilities introduced in Baseline 75.

Existing platform/runtime assets remain in certified locations such as `src/governance`, `src/server`, routing, and integration layers until an approved refactor migrates them safely.
