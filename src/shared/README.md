# Shared Source Assets

Canonical folder for cross-domain shared code introduced in Baseline 75.

Existing shared runtime assets remain in certified locations such as `src/lib`, `src/components/ui`, `src/hooks`, and `src/types` until an approved refactor migrates them safely.
